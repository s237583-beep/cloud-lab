version: '3.8'

services:
  backend:
    build: ./mern-demo
    container_name: mern-backend
    ports:
      - "5000:5000"
    environment:
      - MONGODB_URI=mongodb+srv://cloudlab_voquochuy:Huy%402005@cluster0.7enoyrn.mongodb.net/cloud_lab?appName=Cluster0
      - PORT=5000

  frontend:
    build: ./mern-demo/client
    container_name: mern-frontend
    ports:
      - "4173:4173"
    depends_on:
      - backend
